apiVersion: v1
kind: Service
metadata:
  name: customer-api
spec:
  selector:
    app.kubernetes.io/name: customer-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP