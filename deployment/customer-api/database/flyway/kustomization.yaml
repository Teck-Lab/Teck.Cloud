apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generatorOptions:
  immutable: true
  labels:
    app.kubernetes.io/name: customer-api
    app.kubernetes.io/component: database-migration
resources:
  - job.postgres.yaml
configMapGenerator:
  - name: customer-api-flyway-postgres-sql
    files:
      - .placeholder=./postgres/.placeholder

