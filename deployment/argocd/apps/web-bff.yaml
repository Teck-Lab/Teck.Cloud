apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: web-bff
  namespace: argocd
spec:
  project: teck-cloud
  source:
    repoURL: https://github.com/Teck-Lab/Teck.Cloud.git
    targetRevision: main
    path: deployment/manifests/web-bff
  destination:
    server: https://kubernetes.default.svc
    namespace: teck-cloud
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
