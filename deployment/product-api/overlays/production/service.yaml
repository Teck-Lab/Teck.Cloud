apiVersion: v1
kind: Service
metadata:
  name: product-api
spec:
  selector:
    app.kubernetes.io/name: product-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
    - name: grpc
      appProtocol: kubernetes.io/h2c
      port: 81
      targetPort: grpc
      protocol: TCP
