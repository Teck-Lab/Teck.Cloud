apiVersion: v1
kind: Service
metadata:
  name: location-api
spec:
  selector:
    app.kubernetes.io/name: location-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
