apiVersion: v1
kind: Service
metadata:
  name: statistic-api
spec:
  selector:
    app.kubernetes.io/name: statistic-api
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
