apiVersion: v1
kind: Service
metadata:
  name: admin-gateway
spec:
  selector:
    app.kubernetes.io/name: admin-gateway
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
